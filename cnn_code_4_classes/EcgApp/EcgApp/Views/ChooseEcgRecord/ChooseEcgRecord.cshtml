@model EcgApp.ViewModels.RecordViewModel

@{
    ViewData["Title"] = "Choose ECG record";
}
<h1>@ViewData["Title"]</h1>

<p></p>

<form enctype="multipart/form-data" asp-controller="ChooseEcgRecord" asp-action="Ok">
    <div class="form-group row">
        <label asp-for="RecordFile" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <div class="custom-file">
                <input asp-for="RecordFile" class="form-control custom-file-input">
                <label class="custom-file-label">Choose file..</label>
            </div>
        </div>
    </div>
    <div class="form-group row">
        <div class="col-sm-10">
            <button type="submit" class="btn btn-primary">Ok</button>
        </div>
    </div>

    @section Scripts {
        <script>
            $(document).ready(function () {
                $('.custom-file-input').on("change", function () {
                    var fileName = $(this).val().split("\\").pop();
                    $(this).next('.custom-file-label').html(fileName);
                })
            })
        </script>
    }
</form>